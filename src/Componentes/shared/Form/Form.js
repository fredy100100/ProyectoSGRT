export const Form = (props) => {

    // const {children} = props

    return (
        <div className="form-container">
        <form {...props}>
        {/* {children} */}
        </form>
        </div>
    )
 
}